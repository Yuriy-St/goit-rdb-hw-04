Зміна оператора INNER JOIN на відповідні LEFT чи RIGHT ніяк не впливає на кількість обраних рядків.
В схемі не існує зв'язків з нульовою кардинальністю, таким чином, в базі не можуть існувати FK із значенням NULL, що і є причиною такої поведінки.
